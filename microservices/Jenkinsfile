pipeline {
  agent any
  
 tools {
        maven 'Maven 3.3.9'
    }  

  stages {
     stage("Init"){
	steps {
	      sh '''
	      	  echo "PATH = ${PATH}
		  echo "M2_HOME = ${M2_HOME}"
		  '''
	      }
       }
/*    stage ("Stopping"){
    	steps {
	  sh "docker-compose stop"
	}
  } 
   stage ("Clean") {
    	steps {
	  sh "mvn clean"
	}
   }
   stage ("Install") {
        steps {
	    sh "mvn install"
	  }
    }
    stage ("Package"){
        steps {
	   sh "mvn package"
  	}
    }
    stage ("Deploy Containers") {
    	steps {
	   sh "docker-compose up -d"
 	}
    }
*/
  }
}